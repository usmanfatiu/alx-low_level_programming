#include <stdio.h>
/**
* main - programs that print number below ten starting from zero
* Return: always 0
*/
int main(void)
{
int i;
for (i = 0 ; i < 10 ; i++)
printf("%d", i);
printf("\n");
return (0);
}
